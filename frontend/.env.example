# Vite Environment Variables
# Copy this file to .env and update with your values

# Base path for the application
# For local development, use "/" (root path)
# Only change this if deploying to a subdirectory
VITE_BASE_PATH=/
